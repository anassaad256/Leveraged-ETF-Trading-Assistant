# Data provider: "yahoo" (default, no key needed) or "tiingo"
DATA_PROVIDER=yahoo

# Required only if DATA_PROVIDER=tiingo (free key at https://api.tiingo.com/)
TIINGO_API_KEY=

# Cache TTL in seconds (default 900 = 15 minutes)
CACHE_TTL=900
